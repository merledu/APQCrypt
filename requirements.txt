Flask==2.3.2
pandas>=2.1.1
numpy>=1.26.0,<2.0.0
scikit-learn>=1.3.2
scapy==2.5.0
psutil>=5.9.0
cryptography>=41.0.0
# liboqs-python (ML-KEM / post-quantum crypto) is NOT pip-installable
# as a plain package -- see README for the required system packages
# and install command (it builds a real C library, liboqs, on first import).
